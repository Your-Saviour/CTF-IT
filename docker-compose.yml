services:
  api:
    build: .
    ports:
      - "${API_PORT:-8080}:8000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - api_data:/app/data
    env_file: .env
    environment:
      - DATABASE_URL=sqlite:///data/ctf.db
    restart: unless-stopped

volumes:
  api_data:
