export function createPlannerCanvas(svgElement,callbacks={}){
  const svg=d3.select(svgElement),scene=svg.append('g');
  let graph=[],currentLayout={version:1,nodes:{}};
  svg.call(d3.zoom().scaleExtent([.35,2.4]).on('zoom',event=>scene.attr('transform',event.transform)));
  function defaults(nodes){const positions={};nodes.forEach((row,index)=>{positions[row.id]={x:120+(index%3)*210,y:70+Math.floor(index/3)*110};});return{version:1,nodes:positions};}
  function render(nextGraph,layout={version:1,nodes:{}}){graph=nextGraph;currentLayout={version:1,nodes:{...layout.nodes}};scene.selectAll('*').remove();const nodes=graph.map((row,index)=>({...row,...(currentLayout.nodes[row.id]||{x:120+(index%3)*210,y:70+Math.floor(index/3)*110})})),byId=new Map(nodes.map(row=>[row.id,row]));const links=nodes.filter(row=>row.parent&&byId.has(row.parent)).map(row=>({source:byId.get(row.parent),target:row}));scene.selectAll('line').data(links).join('line').attr('class','topo-link').attr('x1',d=>d.source.x).attr('y1',d=>d.source.y).attr('x2',d=>d.target.x).attr('y2',d=>d.target.y);const groups=scene.selectAll('g.topo-node').data(nodes,d=>d.id).join('g').attr('class',d=>`topo-node ${d.selected?'selected':''}`).attr('data-node-id',d=>d.id).attr('role','button').attr('tabindex',0).attr('aria-label',d=>`${d.type}: ${d.label}`).attr('transform',d=>`translate(${d.x},${d.y})`).on('click',(_,d)=>callbacks.onSelect?.(d.id)).on('keydown',(event,d)=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();callbacks.onSelect?.(d.id);}}).call(d3.drag().on('drag',(event,d)=>{d.x=event.x;d.y=event.y;d3.select(event.sourceEvent.currentTarget).attr('transform',`translate(${d.x},${d.y})`);}).on('end',(_,d)=>{currentLayout.nodes[d.id]={x:d.x,y:d.y};callbacks.onLayoutChange?.(structuredClone(currentLayout));}));groups.append('rect').attr('x',-70).attr('y',-24).attr('width',140).attr('height',48).attr('rx',7);groups.append('text').attr('text-anchor','middle').attr('y',4).text(d=>d.label);}
  function fit(){const bounds=scene.node().getBBox(),width=svgElement.clientWidth||800,height=svgElement.clientHeight||600;if(!bounds.width||!bounds.height)return;const scale=Math.min(1.5,.9/Math.max(bounds.width/width,bounds.height/height)),transform=d3.zoomIdentity.translate(width/2-scale*(bounds.x+bounds.width/2),height/2-scale*(bounds.y+bounds.height/2)).scale(scale);svg.transition().duration(200).call(d3.zoom().transform,transform);}
  function resetLayout(){currentLayout=defaults(graph);callbacks.onLayoutChange?.(structuredClone(currentLayout));render(graph,currentLayout);fit();}
  function focusNode(id){scene.select(`[data-node-id="${CSS.escape(id)}"]`).node()?.focus();}
  function destroy(){svg.on('.zoom',null);scene.remove();}
  return{render,fit,resetLayout,focusNode,destroy};
}
