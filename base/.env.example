# Default root password for the base container image.
# This is the known default that the password_changed verification checks against.
# Copy this file to .env — the .env file must contain ONLY the key=value line (no comments).
ROOT_PASSWORD=changeme123
